# Get a free client_id at https://devportal.jamendo.com/
# Note: this value is baked into the client bundle and is intentionally public —
# it is a usage identifier for Jamendo's free tier, not a secret.
VITE_JAMENDO_CLIENT_ID=your_client_id_here
